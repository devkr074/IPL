function simulateInning(inning,match) {
    const player = JSON.parse(localStorage.getItem('player'));
    const team = JSON.parse(localStorage.getItem('team'));
    console.log(inning,match);
}

export default simulateInning;